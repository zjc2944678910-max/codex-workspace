# ops

This area stores operator-facing material that should not live in project source trees.

- `openclaw/`: OpenClaw-specific operator docs, mirrors, evidence, logs, rollback bundles, and quarantine
