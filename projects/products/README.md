# products

This subtree stores product-style projects.

Use `projects/products/<name>/` for projects that behave like user-facing applications, services, or long-lived product codebases.

Current examples:

- `openclaw/`
- `qigate/`
