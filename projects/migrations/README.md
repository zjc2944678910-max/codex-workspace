# migrations

This subtree stores migration references and import/export transition trees.

Use `projects/migrations/<name>/` for:

- migration bundles
- import/export utilities
- staged reference trees
- historical transition work that should not be confused with the active mainline repository

Current example:

- `openclaw-mac-migration/`
