# infrastructure

This subtree stores infrastructure and platform engineering projects.

Use `projects/infrastructure/<name>/` for:

- deployment stacks
- service topology
- infra automation
- operational platform repositories

Current example:

- `nas-platform/`
