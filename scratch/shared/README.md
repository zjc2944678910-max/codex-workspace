# scratch/shared

This subtree stores temporary material that is not clearly tied to a single project.

Use it for:

- shared experiments
- ad hoc tests
- throwaway notes or outputs
- temporary cross-project helpers

If something becomes project-specific, move it to `scratch/projects/<name>/`.
