# scratch/projects

This subtree stores project-specific temporary material.

Use `scratch/projects/<name>/` for:

- one-off probes
- generated output
- exported assets
- temporary drafts
- short-lived helper scripts

If the material becomes a durable project asset, move it into the corresponding `projects/...` repository.
