# scratch

Temporary outputs belong here.

- `projects/<name>/`: project-specific temporary files and outputs
- `shared/`: shared temporary material not tied to one project

Rule of thumb:

- `scratch/` is for disposable or intermediate material
- if something becomes a real project asset, move it into the appropriate `projects/...` tree
